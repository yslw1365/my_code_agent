这次进步很明显：5.4 的流与最终消息、5.6 的 `messages / terminate`、5.7 的截断调用仍可继续这三块已经基本建立正确理解。

仍需修正的重点如下。

## 还未填写的部分

`5.1 源码入口`仍全部是“待填写”。第五章还不能算完全完成；至少补齐函数名和 `types.ts` 位置，并附上当前 commit。当前源码 commit 仍是 `afae6a1a9`。

## 5.3：消息来源要分三类

你把外层循环理解为“用户追加消息”大致没问题，但从这份文件本身只能确认消息来自配置回调，不能直接断言都是用户输入。

请把这三者区分写清：

- `getSteeringMessages`：Agent 尚在运行时注入到下一轮。
- `getFollowUpMessages`：Agent 原本将结束时才查询。
- 初始 `prompts`：由启动入口加入。

另外，`firstTurn` 不是每次进入 Inner Loop 都重新标记第一轮；它在整个 `runLoop` 期间只会从 `true` 变为 `false` 一次。你现在的“避免重复发出第一轮 `turn_start`”是正确方向。

## 5.4：只有一处措辞需改

你的修正答案正确。只需把：

> 如果当前 turn 是正常开始的……

改为：

> 如果流中收到了 `start` 事件，代码先加入 partial message，结束时再替换为 final message；如果没有收到 `start` 事件，则结束时直接追加 final message。

因为代码并没有把“有无 partial message”定义为“正常/异常”的区分。

## 5.5：第 3、4 题仍有问题

第 3 题里，`toolCalls` 的推导类型不是 `message`，应填写为一个“Tool Call 元素组成的数组”的具体 TypeScript 类型。你已经知道去哪里找了：对照 `executeToolCallsSequential(...)` 中该参数的类型。

第 4 题里，“并行一次循环中完成所有工具执行”不够准确。请查 `ToolExecutionMode` 的注释和 `executeToolCallsParallel(...)`，分别回答：

1. 工具调用的准备阶段是否并行？
2. 真正执行工具的阶段是否并行？
3. `ToolResultMessage` 的发出顺序是否必然等于工具完成顺序？

这样能避免把“并行执行”误解成整个流程全部并行。

## 5.6：正常执行不一定 `terminate: true`

这句需要删除：

> 正常执行返回 true

正常工具批次的 `terminate` 不是固定值。请保留你后面写对的来源：它由 `shouldTerminateToolBatch(...)` 决定；再把它的判断条件写成可验证的完整句子。

另外，“错误日志”最好换成“错误 Tool Result”。日志是否存在是 UI 或事件消费者的行为，而此函数直接可证明的是：它创建并返回一条错误结果消息。

## 5.7：这里还有两处关键修正

### `pendingMessages` 更新时机

表格中的：

> Inner Loop 中 agent_end 后再次检查 SteeringMessages

不对。此处尚未发出 `agent_end`。请改成：

> 在本轮 `turn_end` 后，且 `shouldStopAfterTurn` 没有停止运行时，读取 steering messages。

### `terminate` 的意义

你的补充题 2–4 已答对，但前面第 2 题还留着旧结论：

> `failToolCallsFromTruncatedMessage` ……

这句应删除。截断处理明确返回 `terminate: false`，它不会请求停止后续循环；它只是避免执行参数可能不完整的原始工具，并把错误结果交回模型。

真正可能令工具循环不再自动继续的是：

```
executedToolBatch.terminate === true
```

而正常工具批次的这个值，来自“已完成工具调用的结果是否全部要求 terminate”的判断。

另外，`followUpMessages` 是一次性赋值给 `pendingMessages`，不是“不断填入”。

## 5.8：工具终止这一行仍然错误

这一行把三个概念混在了一起：

- `message.stopReason === "length"`
- `failToolCallsFromTruncatedMessage(...)`
- 工具批次要求终止

它们不是同一条终止路径。

请将“工具执行要求终止”这一行重新追成：

```
工具批次返回 terminate
→ hasMoreToolCalls 被更新
→ 内层循环条件重新判断
→ 再检查 steering
→ 内层退出后再检查 follow-up
→ 最终才可能 agent_end
```

关键结论：

- 截断调用不会直接结束 Agent。
- 工具批次 `terminate: true` 也不会立刻 `return`。
- `return` 的三条路径才会直接退出整个 `runLoop`，不是只退出 Inner Loop。

你关于 `shouldStopAfterTurn` 的疑问，答案是：**这个文件不规定它什么时候返回 `true`。**它由外部调用者配置；此处只负责调用它。若结果为真，当前 turn 会正常收尾，然后发出 `agent_end`，不再读取 steering 或 follow-up，也不再请求模型。

## 5.9：你的直觉正确，混了两层事件

`text_start`、`text_end`、`thinking_*` 不是 `AgentEvent` 的顶层事件；它们属于模型返回的 assistant 流事件，并被包装在 `message_update` 中。

先按 Agent Loop 对外发出的事件写。

场景 A（新 prompt，模型直接回答）的骨架应验证为：

```
agent_start
→ turn_start
→ 用户 message_start / message_end
→ assistant message_start
→ assistant message_update（0 次或多次）
→ assistant message_end
→ turn_end
→ agent_end
```

场景 B 的骨架是：

```
前半段 assistant 回应
→ tool_execution_start / update / end
→ tool result message_start / message_end
→ 第一个 turn_end
→ 第二个 turn_start
→ 第二次 assistant 回应
→ 第二个 turn_end
→ agent_end
```

注意：`agentLoopContinue` 没有新 prompt，因此不一定有“用户 message_start / message_end”。

下一步建议你先修订 5.1、5.5、5.6–5.8，然后完成 5.9。届时第五章就可以算完成，再进入第 6 章“我的理解”。